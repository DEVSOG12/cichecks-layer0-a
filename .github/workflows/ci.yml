name: ci
on:
  pull_request:
jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - run: echo "scan FAIL"; exit 1
  gate:
    needs: scan
    runs-on: ubuntu-latest
    steps:
      - run: echo "gate ran"
